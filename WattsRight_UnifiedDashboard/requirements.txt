# ===========================================
# WattsRight Unified Dashboard - Requirements
# ===========================================
# Install with: pip install -r requirements.txt

# --- Web Frameworks ---
Flask>=3.0.0
Flask-Cors>=4.0.0
Flask-SocketIO>=5.3.0
uvicorn>=0.22.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
Werkzeug>=3.0.0

# --- Machine Learning & Data ---
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scipy>=1.10.0,<1.14.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# --- Deep Learning (optional - for sustainability dashboard) ---
# Note: Install ONE of tensorflow or tensorflow-macos based on your platform
# Windows/Linux: pip install tensorflow>=2.12.0,<2.16.0
# macOS (Apple Silicon): pip install tensorflow-macos>=2.12.0,<2.16.0
tensorflow>=2.12.0,<2.16.0; sys_platform != 'darwin' or platform_machine != 'arm64'
keras>=2.12.0,<3.0.0

# Transformers for LLM pruning
torch>=2.0.0
transformers>=4.30.0

# --- Utilities ---
psutil>=5.9.0
joblib>=1.3.0

# --- GPU Monitoring (Windows/Linux only) ---
# nvidia-ml-py3 is optional and only works on systems with NVIDIA GPUs
# Uncomment if you have an NVIDIA GPU:
# nvidia-ml-py3>=7.352.0

# --- Build Tools (for creating executables) ---
# pyinstaller>=6.0.0
